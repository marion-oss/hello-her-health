# Copy to .env (gitignored). Vite inlines VITE_* vars at build time.

# Backend Worker base URL — points at the BE Worker we deployed in Phase C.
VITE_API_BASE_URL=https://anoqi-api-staging.marion-8c0.workers.dev
